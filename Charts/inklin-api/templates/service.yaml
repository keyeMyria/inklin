apiVersion: v1
kind: Service
metadata:
  name: {{ template "inklin-api.fullname" . }}
  labels:
    app: {{ template "inklin-api.name" . }}
    chart: {{ template "inklin-api.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "inklin-api.name" . }}
    release: {{ .Release.Name }}
